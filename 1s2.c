#include <stdio.h>
#include <conio.h>
void main()
{
    int num, rem;
 
    printf("Input an integer : ");
    scanf("%d", &num);
    rem1 = num % 2;
    if (rem == 0)
    {
        printf("%d is an even integer\n", num);
    }
   else if (rem!=0)
   {
        printf("%d is an odd integer\n", num);
   }
        else
        {
        printf("invalid");
   }
    getch();
        
        
}
